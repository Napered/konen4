기본 비동기 함수 핸들러 내보내기(req, res) {
  // POST 요청만 처리
 만약 (예: method === 'POST') {
 const { message } = req.body;

 만약 (!message) {
 res.status(400).json ({ 오류를 반환합니다: '공지 메시지가 필요합니다.' };
    }

    // 여기서 로블록스 서버와 통신하거나 다른 처리를 할 수 있습니다.
    // 예를 들어, 로블록스에 HTTP 요청을 보내는 코드 작성

    // 공지 메시지 처리 후 성공 응답
 res.status(200).json ({ 성공을 반환합니다: '공지 전송 완료', 메시지: 메시지 });
 } 또 다른 {
    // GET 요청 처리 (옵션)
 res.status(405).json({ 오류: '허용되지 않는 HTTP 메서드입니다.' });
  }
}
